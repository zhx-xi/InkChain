import { test, expect } from "@playwright/test";

// E2E skeleton: ImportManager
// Issue #486
// TODO: fill real E2E code in review/test task

test.describe("ImportManager E2E", () => {
  test("1. Tab switching", async ({ page }) => {
    test.skip();
  })

  test("2. Text paste", async ({ page }) => {
    test.skip();
  })

  test("3. AI extract review", async ({ page }) => {
    test.skip();
  })

  test("4. Confirm save", async ({ page }) => {
    test.skip();
  })
});
